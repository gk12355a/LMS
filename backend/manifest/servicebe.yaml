apiVersion: v1
kind: Service
metadata:
  name: online-lms-backend-service
  namespace: online-lms
spec:
  ports:
    - port: 4000
      protocol: TCP
      targetPort: 4000
  selector:
    app: online-lms-backend

